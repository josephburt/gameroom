/* Optional committed Google Cloud credentials.
   Leave blank and paste them once in the on-page Drive setup dialog
   (saved in this browser). Client IDs are public by design. */
window.GROK_DRIVE_DEFAULTS = {
  clientId: "",
  apiKey: "",
  appId: ""
};
