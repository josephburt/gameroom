{
  "name": "gameroom",
  "private": true,
  "description": "Burt Labs GameRoom — static browser emulator",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
